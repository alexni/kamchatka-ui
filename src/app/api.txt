interface ObjectPassport {
  id: string;
  code?: string;
  name?: string;
  commander?: string;
  deputy?: string;
  paramedic?: string;
  phone?: string;
}

interface Sample {
  id: string;
  code: string;
  name: string;
}

interface ObjectSample {
  id: string;
  object_passport_id: string;
  code: string;
  name: string;
  time_of_receipt: integer; // unix timestamp || DATETIME
  time_of_sampling: integer; // unix timestamp || DATETIME
  target: string;
  place: string;
  value: number;
}

POST /sample
body {
  name: string;
}
response {
  id: string;
  
}

POST /object/passport
body {
  code: string;
  name: string;
  commander: string;
  deputy: string;
  paramedic: string;
  phone: string;
}
response {
  id: string;
}

PUT /object/passport/{id}
body {
  code: string;
  name: string;
  commander: string;
  deputy: string;
  paramedic: string;
  phone: string;
}

POST


